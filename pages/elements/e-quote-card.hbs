<div class="e-quote-card">
    {{> e-icon icon="quote" }}
    <picture>
        <img src="{{ image }}" alt="{{ name }}" />
    </picture>
    <p>{{ quote }}</p>
    <div>
        <strong>{{ name }},</strong>
        <small>{{ position }}</small>
    </div>
</div>
